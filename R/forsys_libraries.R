('Loading required R packages...')
#install.packages('pacman')
pacman::p_load(
  data.table,
  foreign, 
  glue, 
  jsonlite, 
  maptools,
  shiny,
  shinyBS,
  shinyjs, 
  rgdal,
  rgeos,
  roxygen2,
  sp,
  tidyverse
  )
