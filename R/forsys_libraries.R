('Loading required R packages...')
#install.packages('pacman')
pacman::p_load(
  data.table,
  foreign,
  glue,
  gtools,
  jsonlite,
  maptools,
  shiny,
  shinyBS,
  shinyjs,
  stringr,
  rgdal,
  rgeos,
  roxygen2,
  sp,
  tidyverse
  )

library(Patchmax)
